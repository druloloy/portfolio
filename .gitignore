build
dist
/media
node_modules
.DS_Store
.env
.next
.vercel
keystore/keyfile.*.json
# TypeScript incremental build cache
*.tsbuildinfo

# Planning and audit docs are kept locally, not shipped
docs/superpowers/

# Local agent/editor tooling config, not part of the product
.claude/

# Local database backups live outside the repo; never commit one
portfolio-backups/
